export interface Config {
  name: string
  username: string
  hostname: string
  email: string
  linkedin: string
  github: string
  location: string
  headline: string
  colors: {
    user: string
    root: string
    host: string
    caret: string
    primary: string
    text: string
    background: string
    selection: string
  }
}
